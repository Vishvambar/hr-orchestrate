# Official challenge drop zone

When HackerRank sends the prompt:

1. save the official files in this directory
2. copy `configs/challenge.template.toml` to `challenge/current/contract.toml`
3. update the contract to match the exact evaluator schema
4. keep bulky raw assets here and let generated outputs go to `artifacts/runs/`
